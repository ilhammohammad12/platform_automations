worker_processes auto;
events {
    worker_connections 1024;
}
http {
    server {
        listen 80;
        server_name {{ ansible_host }};
        location / {
            root /var/www/html;
            index index.html;
        }
    }
}
